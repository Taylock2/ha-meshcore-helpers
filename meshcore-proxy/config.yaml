name: "MeshCore Proxy"
description: "TCP proxy for MeshCore radios via USB serial connection"
version: "1.0.14"
slug: "meshcore_proxy_addon"
arch:
  - aarch64
  - amd64
usb: true
uart: true
ports:
  5000/tcp: 5000
options:
  serial_port: ""
  baud_rate: 115200
schema:
  serial_port: "str?"
  baud_rate: "int?"